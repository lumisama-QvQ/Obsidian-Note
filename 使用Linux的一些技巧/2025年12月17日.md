# 解决在`XWayland`下输入法不生效的问题

在`Wayland`下`X11`的软件不会自动沿用相关配置，这就需要我们手动配置相关的**环境变量**
在`/etc/environment`文件中写入：
```bash
XMODIFIERS=@im=fcitx
QT_IM_MODULE=fcitx
SDL_IM_MODULE=fcitx #针对steam
#GTK_IM_MODULE=fcitx #不建议开启，会yin xi
```