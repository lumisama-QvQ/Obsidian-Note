## 泛型是什么
通俗的讲，就是一种给程序员们偷懒的方式，用一个函数来处理不同的数据
```rust
fn add<T>(a: T, b: T) -> T {
	a + b
}
```
这里的`T`便是**泛型参数**，出于惯例我们习惯用`T`[^1]作为首选，实际在Rust中用什么都可以

## 泛型的基本使用
泛型在使用前**必须声明**：
```rust
fn add<T>(a: T, b: T) -> T 
```
`add<T>`先对泛型`T`声明，之后再
[^1]: Type
